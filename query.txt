curl -X GET "http://localhost:9200/foods/_search?pretty" \
  -H "Content-Type: application/json" \
  -d '{
    "query": {
      "bool": {
        "must": [
          {
            "match": {
              "name": {
                "query": "Pineapple",
                "fuzziness": "AUTO"
              }
            }
          }
        ],
        "filter": [
          {
            "term": {
              "date": "'$(date +%Y-%m-%d)'"
            }
          }
        ]
      }
    },
    "collapse": {
      "field": "name.keyword"
    }
  }'